import {describe,it,expect,beforeEach} from 'vitest'; import {calculateCartTotals,addToCart,getCartItems,updateQuantity,clearCart} from '../src/js/cart.js'; beforeEach(()=>clearCart());describe('cart',()=>{it('adds and totals active items',()=>{addToCart('p','v','افتراضي',100,'','منتج');updateQuantity('v',2);expect(calculateCartTotals(getCartItems())).toMatchObject({subtotal:200,itemCount:2})});it('excludes stale items',()=>expect(calculateCartTotals([{price:4,quantity:2,is_stale:true}]).subtotal).toBe(0))})
