# Security Debt

قرارات أمنية مؤجّلة عمدًا مع سبب التأجيل وشروط العودة ليها. الهدف إن أي حد
يفتح الملف ده يعرف ليه الحاجة دي لسه موجودة وإمتى نتحرك تجاهها، بدل ما تتنسى.

## CSP: unsafe-inline (من تقرير /cso — Finding #5)

**الحالة**: مؤجّل عمدًا، مش منسي.

**المشكلة**: 10 صفحات فيها inline scripts خاصة بيها، وCSP الحالي فيه `unsafe-inline`
مفعّل عشانها.

**ليه اتأجل**: شيل `unsafe-inline` محتاج إما:
- Hash لكل script (بيتكسر مع أي تعديل مستقبلي بسيط في الكود)
- أو نقل كل الـ business logic لملفات خارجية (refactor كبير وخطر عبر 10 صفحات)

الـ XSS الأساسي اللي كان سبب المشكلة اتقفل من جذوره بالفعل في هذه المرحلة،
يعني `unsafe-inline` دلوقتي طبقة دفاع إضافية (defense-in-depth) مش الحاجز
الوحيد ضد XSS.

**امتى نرجعلها**:
- لو هنضيف صفحة دفع أو حسابات مستخدمين جديدة
- لو حصل أي محاولة XSS فعلية (حتى لو اتقفلت)
- كخطوة تحسين تدريجية لاحقة (صفحة واحدة في كل مرة، مش كلهم مرة واحدة)

**تاريخ القرار**: 2026-08-26
