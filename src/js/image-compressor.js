const allowed=['image/jpeg','image/png','image/webp'];
export async function compressImage(file,{maxSize=2*1024*1024,maxDimension=1200}={}){if(!allowed.includes(file.type))throw new Error('الصور المقبولة هي JPEG وPNG وWebP فقط.');if(file.size<=maxSize)return file;const source=await createImageBitmap(file);const scale=Math.min(1,maxDimension/source.width,maxDimension/source.height);const canvas=document.createElement('canvas');canvas.width=Math.round(source.width*scale);canvas.height=Math.round(source.height*scale);canvas.getContext('2d').drawImage(source,0,0,canvas.width,canvas.height);const blob=await new Promise(resolve=>canvas.toBlob(resolve,'image/webp',.8));if(!blob||blob.size>maxSize)throw new Error('تعذر ضغط الصورة إلى أقل من 2MB.');return new File([blob],file.name.replace(/\.[^.]+$/,'.webp'),{type:'image/webp'});}
